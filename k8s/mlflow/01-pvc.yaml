apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mlflow-storage
  namespace: rag
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # just the sqlite file and a few small artifacts not the model weights
      storage: 2Gi
